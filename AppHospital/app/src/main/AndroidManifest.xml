<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.upb.apphospital">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Creditos"
            android:label="Creditos A" />
        <activity
            android:name=".SinPosibleCovid"
            android:label="Sin Covid" />
        <activity
            android:name=".PosibleCovid"
            android:label="Posible Covid" />
        <activity
            android:name=".TiempoSintomas"
            android:label="Tiempo Con Sintomas" />
        <activity
            android:name=".InformacionHospital"
            android:label="Informacion del Hospital" />
        <activity
            android:name=".ServiciosOfrecidosHosp"
            android:label="Servicios Ofrecidos Hospital" />
        <activity
            android:name=".SintomasCovid"
            android:label="Sintomas Covid" />
        <activity
            android:name=".ServiciosHospital"
            android:label="Servicios del Hospital" />
        <activity
            android:name=".ElegirHospital"
            android:label="Elegir un Hospital" />
        <activity
            android:name=".ubicacionActual"
            android:label="@string/title_activity_ubicacion_actual" />
        <activity
            android:name=".IngresarDatos"
            android:label="Ingresar Datos" />

        <activity android:name=".Login"
            android:label="Registrase"/>

        <activity
            android:name=".PantallaDeCarga"
            android:label="Encuentra tu salud">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            />
        </activity>
    </application>

</manifest>